<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/simple_toast_bg"
    android:gravity="center"
    android:orientation="vertical" >

    <ProgressBar
        android:id="@+id/simple_dialog_progress_bar"
        style="?android:attr/progressBarStyle"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_margin="5dip"
        android:indeterminateDrawable="@drawable/global_progress_normal"
        android:visibility="visible" />

    <TextView
        android:id="@+id/simple_dialog_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:text="正在处理中..."
        android:textSize="14sp"
        android:textColor="@color/co_text_white" />

</LinearLayout>